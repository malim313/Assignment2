Thd code first untars the folder and then sets the working directory.

Next, the code reads the three relevant .txt files.
Patient IDs are matched and the metadata is built.

The data is then normalized.
